# 何国华 - 面试问答总结

> 简历核心 + 标准回答模板

---

## 一、简历核心亮点（3分钟速记）

### 基本信息
| 项目 | 内容 |
|-----|------|
| 姓名 | 何国华 |
| 工作年限 | 4年 |
| 最近公司 | 深圳世纪众云科技有限公司 |
| 求职方向 | 前端开发（Vue方向） |

### 技术栈
```
Vue3 + TypeScript + Pinia + Element Plus + Vite
微信小程序 / uniapp + 小程序开发
```

### 三个核心项目

| 项目 | 时间 | 亮点 |
|-----|------|-----|
| 低代码平台 | 2025-至今 | 表单设计器、虚拟滚动、Schema规范 |
| 运连网财务系统 | 2023-2025 | 0-1架构搭建、RBAC权限 |
| 汇佰商城小程序 | 2022-2023 | 独立主导、支付流程、性能优化 |

### 核心数据（必须记住）

| 数据 | 含义 |
|-----|------|
| 30+ | 支撑的业务表单数量 |
| 80% | 表单开发效率提升（2天→2小时） |
| 100+ | 画布组件数量时的卡顿问题 |
| 60% | 返工率减少（Schema规范） |
| 12 | 封装的业务组件数量 |
| 20+ | 统一Schema的组件数量 |
| 1.8M | 小程序主包体积 |
| 30% | 首屏渲染优化 |
| 95%+ | 支付成功率 |

---

## 二、自我介绍（标准版）

**模板：**

```
面试官好，我叫何国华，4年前端经验。

上一家公司在深圳世纪众云科技，主要做企业管理系统和低代码平台。

我擅长 Vue 全家桶、TypeScript、小程序开发。

最有代表性的项目是低代码平台。我负责表单设计器模块，支撑了 30 多个业务表单，效率提升 80%。同时解决了 100+ 组件渲染卡顿问题，用虚拟滚动优化，DOM 减少 80%。我也设计了组件 Schema 规范，统一了 20 多个组件的数据格式。

另一个项目是运连网财务系统，我主导了项目从 0 到 1 的架构搭建，用 Vue3 + Vite + Pinia 技术栈，基于 RBAC 实现了权限系统。

还有汇佰商城小程序，我独立完成了商品、购物车、支付等核心功能，主包体积控制在 1.8M，首屏优化提升 30%。

我具备独立负责项目的能力，有良好的代码规范意识。

谢谢。
```

**时长：1分30秒**

---

## 三、高频问答模板（直接背）

### Q1：你负责低代码平台哪部分？

```
我主要负责表单设计器模块，包括：
1. 表单设计器的整体架构（拖拽生成、属性配置、保存渲染）
2. 12个业务组件的封装（输入框、下拉、表格、搜索面板等）
3. 画布的虚拟滚动优化（解决100+组件渲染卡顿）
4. 组件的 Schema 数据格式规范（统一前后端数据）

表单设计器支撑了30多个业务表单，把表单开发效率从原来平均2天提升到2小时，提升了80%。
```

---

### Q2：虚拟滚动是什么？

```
核心原理是窗口化渲染，只渲染用户能看到的内容。

之前的问题是：画布上组件超过100个时，页面明显卡顿。原因是把所有组件都渲染到DOM里了。

我的解决方案是：只渲染可视区域内的组件。比如用户当前只能看到5个组件，那就只渲染5个，用padding-top撑开高度，保持滚动条正常。

配合的技术细节：
1. 用 Map 存储每个组件的实际高度（因为组件高度不固定）
2. 滚动时用 requestAnimationFrame 节流
3. 缓冲区多渲染2-3个，防止快速滚动白屏
```

---

### Q3：为什么不用第三方库？

```
我调研过 vue-virtual-scroller，但不适合我们的场景：
1. 它需要固定高度，但我们组件高度不固定
2. 不支持拖拽排序时动态调整可视范围
3. 不支持嵌套容器

所以我自己实现了一套，支持动态高度和拖拽排序。
```

---

### Q4：Schema 规范是什么？

```
Schema 就是组件的数据结构规范。我定义了统一的格式：

{
  id: 'block_001',
  code: 'AlaInput',
  name: '用户名',
  formData: {
    desktop: { placeholder: '请输入', prop: 'username' },
    mobile: { placeholder: '请输入', prop: 'username' }
  }
}

好处是：
1. 前后端约定好格式，减少对接返工（返工率减少60%）
2. 支持响应式（desktop 和 mobile 配置分开）
3. 20多个组件都统一这套格式
```

---

### Q5：效率提升80%怎么算的？

```
原来是估算的：
传统开发一个表单：创建页面2h + 写组件4h + 配置接口4h + 调样式2h + 调试2h ≈ 14-16h
用设计器配置：选择组件5min + 配置属性10min + 绑定接口20min + 微调10min ≈ 1h

(16-1)/16 ≈ 94%，保守说80%

实际上支撑了30多个表单，累计节省时间 (16-1) × 30 = 450小时。
```

---

### Q6：团队几个人？你负责什么？

```
低代码平台3个人：
- 我：表单设计器、虚拟滚动、Schema规范
- 同事A：页面设计器、大屏设计器
- 同事B：后端API、数据存储、权限控制

运连网财务系统：
- 我主导 0-1 架构搭建，路由、状态管理、公共组件封装

汇佰商城小程序：
- 我独立主导，从0到1完成全部开发
```

---

### Q7：Vue3 和 Vue2 有什么区别？

```
1. 响应式原理变了
   - Vue2 用 Object.defineProperty，只能监听已有属性
   - Vue3 用 Proxy，可以监听新增/删除属性

2. Composition API
   - Vue2 是 Options API，逻辑分散在 data/methods/computed 里
   - Vue3 是 Composition API，相关逻辑可以抽到一个函数里

3. 性能优化
   - Vue3 编译时优化，静态节点提升
   - Vue3 有更好的 tree-shaking 支持

4. 体积
   - Vue3 体积更小

我项目中主要用 Vue3 + Composition API + setup 语法糖。
```

---

### Q8：TypeScript 有什么好处？

```
1. 类型检查
   - 编译时发现错误，不用等到运行
   - 比如字段名写错，IDE 直接提示

2. 代码提示
   - IDE 自动补全，提高开发效率

3. 接口定义
   - Schema 规范用 TypeScript 定义，前后端共享类型

实际收益：
// 之前：运行时才发现字段写错
config.mobileWidth  // 运行时才发现是 undefined

// 现在：编译时就报错
interface Config {
  formData: { mobile: { width: string } }
}
```

---

### Q9：Pinia 和 Vuex 有什么区别？

```
Pinia 是 Vue3 官方推荐的，更轻量：
1. API 更简洁，不用 mutations 了
2. TypeScript 支持更好
3. 体积更小（1KB vs 较大）
4. 支持组合式 API

我项目里这样用：
const editorStore = defineStore('editor', () => {
  const blockConfig = ref([])
  const currentSelect = ref({})
  return { blockConfig, currentSelect }
})
```

---

### Q10：项目中最难的问题是什么？

```
最难的是虚拟滚动的动态高度问题。

问题：
1. 画布组件高度不固定
2. 支持拖拽排序（组件会换位置）
3. 支持嵌套容器

尝试的方案：
1. 固定高度 → 不行，组件高度差异大
2. 第三方库 → 不行，不支持拖拽嵌套
3. 自己实现 → 用 Map 存储每个组件的实际高度

最终方案：
- 初始用预估高度（120px）
- 组件挂载后测量实际高度，存入 Map
- 滚动时用 Map 里的高度计算位置
- 拖拽排序时更新受影响组件的高度
```

---

### Q11：你在这个项目的价值？

```
我主要做了三件事：

1. 从0到1搭建了表单设计器
   - 包括拖拽生成、属性配置、保存渲染的完整链路

2. 制定了组件 Schema 规范
   - 统一了20+个组件的数据格式
   - 减少了前后端对接的返工

3. 解决了虚拟滚动难题
   - 自研动态高度方案
   - 支撑了30+个业务表单上线
```

---

### Q12：遇到过最难的技术挑战？

```
有两个印象深刻的：

1. 低代码平台渲染卡顿
   - 100+组件时明显卡顿
   - 用虚拟滚动解决，需要考虑动态高度、拖拽排序、嵌套容器

2. 支付结果同步
   - 微信支付后结果回调有时延
   - 设计了短轮询机制，3秒内轮询5次
   - 支付成功率提升到95%以上
```

---

## 四、回答技巧总结

### ✅ 正确姿势

| 技巧 | 说明 |
|-----|------|
| 结论先行 | 先说结论，再解释原因 |
| 量化数据 | 30+、80%、100+ 这些数字要说 |
| STAR法则 | 情境→任务→行动→结果 |
| 留追问空间 | 说到亮点时停顿，等面试官问 |
| 用自己的话 | 不要直接念简历 |

### ❌ 错误姿势

| 错误 | 改正 |
|-----|------|
| 背书式回答 | 语气自然，像聊天 |
| 流水账 | 只说做了什么，不说结果 |
| 夸大数字 | 30+ 就说 30+，不要说 100+ |
| 不知道装知道 | 说"这个我了解不深" |

### 📋 回答结构模板

```
问题 → 
先说结论（比如：虚拟滚动、Schema规范、RBAC）
→ 解释背景（遇到了什么问题）
→ 我的方案（具体怎么做的）
→ 结果（效率提升多少、减少了多少问题）
→ 数字支撑（30+、80%、100+）
```

---

## 五、快速自检清单

面试前过一遍：

```
□ 自我介绍能背下来（1分30秒）
□ 3个核心项目能说清楚
□ 30+、80%、100+ 这些数字要记牢
□ 虚拟滚动原理能说清楚
□ Schema 规范是什么要会解释
□ Pinia vs Vuex 的区别
□ TypeScript 的好处
□ Vue3 vs Vue2 的区别
□ 最难的问题是什么
□ 你在项目的价值
□ 团队几个人、分别负责什么
```

---

**祝面试顺利！**
